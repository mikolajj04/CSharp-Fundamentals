Task:

Implement a custom parser method.

The parser checks whether the value entered by the user can be parsed into an integer
and whether that integer is a negative number.
If the value is valid, the method returns the parsed integer;
otherwise, it returns null.